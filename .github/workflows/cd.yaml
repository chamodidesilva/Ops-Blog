name: CD

on:
    workflow_dispatch:
    workflow_run:
        workflows: ["CI"]
        types: [completed]

jobs:
    Deploy:
        runs-on: ubuntu-latest
        if: ${{ github.event.workflow_run.conclusion == 'success' }}
        steps:
            - name: Register CD workflow
              run: |
                echo "Trigger registered on main."
                exit 0